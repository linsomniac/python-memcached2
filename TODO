Make "get" accept multiple keys (see "get <key>*" in protocol docs).
Make another wrapper class that catches exceptions.
Post socket server for unit test to let others know about it.
Decide on automatic pickling.
